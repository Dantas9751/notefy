# Deixe vazio em desenvolvimento: o Vite faz proxy de /api para o Django
# (ver vite.config.js), o que evita CORS. Defina apenas se o backend
# estiver em outro host.
# VITE_API_URL=https://api.seudominio.com/api
